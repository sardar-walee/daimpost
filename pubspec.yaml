name: daimpost_v1_6
description: Daimpost Flutter V1.6
publish_to: "none"
version: 1.6.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  shared_preferences: ^2.2.3
  http: ^1.2.1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.18.0

dev_dependencies:
  shared_preferences: ^2.2.3
  http: ^1.2.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/translations/
